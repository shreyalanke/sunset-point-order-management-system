/**
 * @type {import('node-pg-migrate').ColumnDefinitions | undefined}
 */
export const shorthands = undefined;

/**
 * @param pgm {import('node-pg-migrate').MigrationBuilder}
 * @param run {() => void | undefined}
 * @returns {Promise<void> | void}
 */
export const up = (pgm) => {
    pgm.sql(`ALTER TABLE order_items
ADD COLUMN dish_name_snapshot VARCHAR(100) NOT NULL,
ADD COLUMN price_snapshot INTEGER NOT NULL CHECK (price_snapshot > 0);`)
};

/**
 * @param pgm {import('node-pg-migrate').MigrationBuilder}
 * @param run {() => void | undefined}
 * @returns {Promise<void> | void}
 */
export const down = (pgm) => {
    pgm.sql(`ALTER TABLE order_items
DROP COLUMN dish_name_snapshot,
DROP COLUMN price_snapshot;`);
};
